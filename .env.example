# =============================================================================
# UserService Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values for local development.
# For Azure deployment, set these in Azure App Service Configuration or use
# Azure Key Vault and Azure App Configuration.
# =============================================================================

# -----------------------------------------------------------------------------
# Azure Configuration (Optional - for cloud deployments)
# -----------------------------------------------------------------------------
# Azure Key Vault URI for secrets management
AZURE_KEY_VAULT_URI=https://your-keyvault-name.vault.azure.net/

# Azure App Configuration connection string for centralized config
AZURE_APP_CONFIGURATION_CONNECTION_STRING=Endpoint=https://your-appconfig.azconfig.io;Id=xxx;Secret=xxx

# -----------------------------------------------------------------------------
# Database Connection
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: Host=hostname;Database=dbname;Username=user;Password=pass;SSL Mode=Require
ConnectionStrings__PostgresConnection=Host=localhost;Port=5432;Database=userservice;Username=postgres;Password=yourpassword;SSL Mode=Prefer

# -----------------------------------------------------------------------------
# Auth0 Configuration
# -----------------------------------------------------------------------------
# Auth0 tenant domain (e.g., your-tenant.auth0.com)
Auth0__Domain=your-tenant.auth0.com

# Auth0 API audience/identifier
Auth0__Audience=https://your-api-identifier

# Auth0 application client credentials
Auth0__ClientId=your-client-id
Auth0__ClientSecret=your-client-secret

# Auth0 Management API credentials (for user creation, role assignment)
Auth0__MgmtClientId=your-mgmt-client-id
Auth0__MgmtClientSecret=your-mgmt-client-secret
Auth0__ManagementAudience=https://your-tenant.auth0.com/api/v2/

# Auth0 database connection name
Auth0__DbConnection=Username-Password-Authentication

# Auth0 role IDs (get these from Auth0 dashboard)
Auth0__Roles__BusinessUser=rol_xxxxxxxxxx
Auth0__Roles__SupportUser=rol_xxxxxxxxxx
Auth0__Roles__EndUser=rol_xxxxxxxxxx

# -----------------------------------------------------------------------------
# External Services
# -----------------------------------------------------------------------------
# Business Service API base URL (use HTTPS in production!)
Services__BusinessServiceBaseUrl=https://businessservice.aerglotechnology.com

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins (for local development)
# In production, set specific origins in Azure App Configuration
Cors__AllowedOrigins__0=http://localhost:3000
Cors__AllowedOrigins__1=http://localhost:5173

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Enable Swagger UI (set to false in production)
EnableSwagger=true

# ASP.NET Core environment
ASPNETCORE_ENVIRONMENT=Development
